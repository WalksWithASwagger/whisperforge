---
description: 
globs: 
alwaysApply: false
---
- Reject patterns /(key|token|secret|password)\s*=\s*['"][A-Za-z0-9_\-]{16,}/
- All credentials must be read from environment variables or ~/.config
- Use os.getenv() or os.environ.get() for sensitive configuration
- Never commit lines that match the secret pattern above

